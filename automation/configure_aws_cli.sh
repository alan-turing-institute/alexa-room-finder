# Commands required to configure AWS CLI

brew install awscli

# pip install awscli

aws configure set aws_access_key_id {AWS KEY}
aws configure set aws_secret_access_key {AWS SECRET}
aws configure set default.region eu-west-1
